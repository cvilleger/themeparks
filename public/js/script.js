$(function() {

    // $('table').DataTable( {
    //     "data": '/entries'
    // } );

    // $.get( "/entries", function( entries ) {
    //     $('.no-records-found').remove();
    //     var html = '';
    //     entries.forEach(function( entry ) {
    //         html =
    //             '<tr>'
    //             + '<td>' + entry.name + '</td>'
    //             + '<td>' + entry.waitTime.postedWaitMinutes + '</td>'
    //             + '<td>' + entry.waitTime.fastPass.available + '</td>'
    //             + '<td>' + entry.waitTime.singleRider + '</td>'
    //             + '<td>' + entry.waitTime.status + '</td>'
    //             + '</tr>'
    //         ;
    //         $( "tbody" ).append(html);
    //     });
    // });

});
